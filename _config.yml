markdown: redcarpet
sass:
  style: :compressed
exclude: ["CNAME", "Gemfile*", "README.md"]
destination: _site
branch: source
defaults:
  -
    scope:
      path: "" # addresses every file
    values:
      layout: default
